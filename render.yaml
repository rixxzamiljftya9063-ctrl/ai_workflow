services:
  - type: web
    name: ai-workflow-backend
    runtime: python
    plan: free
    rootDir: backend
    buildCommand: python -m pip install --upgrade pip && python -m pip install -r requirements.txt
    startCommand: python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: DATABASE_URL
        value: sqlite:///./storage/ai_workflow.db
      - key: STORAGE_ROOT
        value: ./storage
      - key: FRONTEND_ORIGIN
        value: https://rixxzamiljftya9063-ctrl.github.io
      - key: DEFAULT_MOCK_PROVIDER
        value: Mock Provider
      - key: REQUEST_TIMEOUT_SECONDS
        value: 180
      - key: PROVIDER_RETRY_MAX_ATTEMPTS
        value: 5
      - key: PROVIDER_RETRY_BASE_DELAY_SECONDS
        value: 1
      - key: PROVIDER_RETRY_MAX_DELAY_SECONDS
        value: 8
